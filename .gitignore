# Ignore Codex local runtime state while allowing future tracked project config.
/.codex/*
!/.codex/config.toml
!/.codex/rules/
!/.codex/rules/*.rules

# Authenticated OCI CLI exports are local verification inputs, not site assets.
native-model-matrix.json
